# README4vsixUsers

## 安装与配置

### 1. 下载扩展
下载最新版本的 `.vsix` 文件。

### 2. 安装扩展
在 `AnalyseNG.NET` 文件夹中打开 VS Code。

按下 `Ctrl+Shift+P`，输入：

`Extensions: Install from VSIX`

然后选择已经下载好的 `.vsix` 文件进行安装。

安装完成后，再按下 `Ctrl+Shift+P`，输入：

`Developer: Reload Window`

并执行。

### 3. 运行配置向导
按下 `Ctrl+Shift+P`，输入：

`TSL: Setup Wizard`

如果扩展能够自动找到正确路径，则按照向导填写地址、端口、账号和密码即可。

如果扩展无法自动找到正确路径，则需要手动填写正确的 Python 路径和 `AnalyseNG.NET` 路径。通常出现在以下情况：

- `AnalyseNG.NET` 位于 `C:` 盘
- `PATH` 中没有 Python
- 打开的 VS Code 工作目录不是 `AnalyseNG.NET`

### 4. 完成配置
当左下角出现绿色的 `TSL Ready` 时，说明配置已经完成。

此时新建一个 `.tsl` 文件即可开始编写。  
按下：

`TSL: Validate`

即可运行校验。

## 可选步骤

### 可选步骤 1：验证环境
可以使用本仓库中的 `LocalTest.tsl` 和 `OnlineTest.tsl` 来验证配置是否正确完成。

### 可选步骤 2：与 Codex 协同
按下：

`TSL: Open in Codex`

即可与 Codex 协同进行编程。
