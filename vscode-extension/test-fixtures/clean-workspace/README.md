This folder is a minimal clean-workspace fixture used by extension host integration tests.

- `sample.tsl` is used for lint/smoke/codex command smoke checks.
- Workspace settings are applied dynamically during test runtime.

